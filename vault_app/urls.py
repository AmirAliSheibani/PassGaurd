from django.urls import path

# from .views import VaultDetailView


app_name = "vault_app"

# urlpatterns = [
#     path("<str:username>/<slug:vault_slug>/", VaultDetailView.as_view(), name="detail"),
# ]